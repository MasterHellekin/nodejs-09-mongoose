"use strict";var express=require("express"),_require=require("express-validator/check"),check=_require.check,body=_require.body,authController=require("../controllers/auth"),User=require("../models/user"),router=express.Router();router.get("/login",authController.getLogin),router.get("/signup",authController.getSignup),router.post("/login",[body("email").isEmail().withMessage("Please enter a valid email address").normalizeEmail(),body("password","Password has to be valid").isLength({min:5}).isAlphanumeric().trim()],authController.postLogin),router.post("/signup",[check("email").isEmail().withMessage("Please enter a valid email").custom(function(e,r){r.req;return User.findOne({email:e}).then(function(e){if(e)return Promise.reject("E-mail exist already, please pick a different one")})}).normalizeEmail(),body("password","Please enter a password with only numbers and text and a least 5 characters").isLength({min:4}).isAlphanumeric().trim(),body("confirmPassword").trim().custom(function(e,r){if(e!==r.req.body.password)throw new Error("Passwords have to match!");return!0})],authController.postSignup),router.post("/logout",authController.postLogout),router.get("/reset",authController.getReset),router.post("/reset",authController.postReset),router.get("/reset/:token",authController.getNewPassword),router.post("/new-password",authController.postNewPassword),module.exports=router;